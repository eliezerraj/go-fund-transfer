apiVersion: v1
kind: Service
metadata:
  name: &app-name svc-go-fund-transfer
  namespace: test-a
  labels:
    app: *app-name
spec:
  type: ClusterIP
  ports:
  - port: 5001
    targetPort: 5001
    protocol: TCP
    name: http
  selector:
    app: go-fund-transfer
